<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup Label="Build">
        <TargetFrameworks>netstandard2.1;net462;net8.0;net10.0</TargetFrameworks>
        <LangVersion>latest</LangVersion>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
    </PropertyGroup>

    <PropertyGroup Label="Package">
        <Version>2.0.0</Version>
        <Title>Cel.NET</Title>
        <Authors>Robert Yokota</Authors>
        <Description>Cel.NET is an implementation of the Common Expression Language (CEL) for .NET</Description>
        <PackageId>Cel.NET</PackageId>
        <PackageTags>google;common;expression;language;cel</PackageTags>
        <PackageProjectUrl>https://github.com/rayokota/cel.net</PackageProjectUrl>
        <PackageLicenseExpression>Apache-2.0</PackageLicenseExpression>
        <PackageReadmeFile>README.md</PackageReadmeFile>
        <RepositoryUrl>https://github.com/rayokota/cel.net</RepositoryUrl>
        <PublishRepositoryUrl>true</PublishRepositoryUrl>
    </PropertyGroup>

    <PropertyGroup Label="Signing">
        <SignAssembly>true</SignAssembly>
        <AssemblyOriginatorKeyFile>..\..\Cel.snk</AssemblyOriginatorKeyFile>
    </PropertyGroup>

    <ItemGroup>
        <None Remove="generated-antlr/CEL.g4" />
        <None Include="..\..\README.md" Pack="true" PackagePath="\" />
    </ItemGroup>
    
    <ItemGroup>
        <Antlr4 Include="generated-antlr/CEL.g4">
            <AntOutDir>generated-antlr/build/generated-src/Cel/Parser/Gen</AntOutDir>
            <Listener>false</Listener>
            <Package>Cel.Parser.Gen</Package>
        </Antlr4>
    </ItemGroup>

    <ItemGroup>
        <PackageReference Include="Antlr4.Runtime.Standard" Version="4.13.1" />
        <!-- Build-time grammar codegen; keep it out of consumers' dependency closure, like
             Grpc.Tools below. -->
        <PackageReference Include="Antlr4BuildTasks" Version="12.14.0">
            <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
            <PrivateAssets>all</PrivateAssets>
        </PackageReference>
    </ItemGroup>

    <ItemGroup>
        <Protobuf Include="generated-pb/**/*.proto" GrpcServices="None" OutputDir="generated-pb/build/generated-src" ProtoRoot="generated-pb/googleapis" />
        <Protobuf Update="generated-pb/**/*_service.proto" GrpcServices="Both" />
    </ItemGroup>

    <ItemGroup>
        <PackageReference Include="Apache.Avro" Version="1.12.1" />
        <PackageReference Include="Newtonsoft.Json" Version="13.0.4" />
        <PackageReference Include="NodaTime" Version="3.3.3" />
        <PackageReference Include="Google.Protobuf" Version="3.26.1" />
        <!-- Only the generated ConformanceService gRPC stub needs this, and only the Grpc.Core
             abstractions (ClientBase/Marshaller) it references at compile time. -->
        <PackageReference Include="Grpc.Core.Api" Version="2.63.0" />
        <PackageReference Include="Grpc.Tools" Version="2.63.0">
            <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
            <PrivateAssets>all</PrivateAssets>
        </PackageReference>
    </ItemGroup>

    <Target Name="Clean">
        <RemoveDir Directories="generated-antlr/build" />
        <RemoveDir Directories="generated-pb/build" />
    </Target>

</Project>

